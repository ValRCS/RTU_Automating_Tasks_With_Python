# let's load pandas library and show its version
import pandas as pd # standard alias for pandas is pd
print(f"Pandas version: {pd.__version__}")
# the script is over here
# if we wanted something interactive we would want to use Jupyter Notebooks instead